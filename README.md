# File-Tree-Viewer
A simple application written in python to view the file structure of a directory and its subdirectories. 

Run the application with Python on the launcher or the file_tree_viewer main file directly if you already have PyQt6 installed.
